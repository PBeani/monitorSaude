import React from 'react';
import './Header.css';

const Header = props => {
    const { routes, path } = { ...props };

    return (
        <header>
        </header>
    )
};

export default Header;